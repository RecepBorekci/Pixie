import 'package:flutter/material.dart';

class EditedPhoto extends StatelessWidget {
  final photoURL;
  final timeStamp;

  EditedPhoto({this.photoURL, this.timeStamp});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
